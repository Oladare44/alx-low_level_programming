#include <stdio.h>
/**
 * main : Main Entry
 *
 * Return: return 0 - Successful
 */
int main(void)
{
	char c;

	char = 'a';
	while 
		(c <= 'Z') {
			if ((c != 'q' && c != 'e') && c <= 'z')
				putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}

